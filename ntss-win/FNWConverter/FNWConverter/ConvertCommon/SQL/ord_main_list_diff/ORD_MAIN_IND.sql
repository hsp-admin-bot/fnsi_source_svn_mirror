WITH  CONVERT_HISTORY AS {0} 

SELECT  PATID FROM (
SELECT
     PATID
FROM
    IND_DEVELOP_ADD S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME  GROUP BY PATID
UNION ALL
SELECT
      S.PATID
FROM
    IND_DEVELOP_COND S 
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
   S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
     PATID
FROM
    IND_DEVELOP_EQUIP S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
     PATID
FROM
    IND_DEVELOP_MEDI S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
     PATID
FROM
    IND_DEVELOP_PLAN  S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
	 R.PATID 
FROM
	(
	SELECT
		PATID,
		MIN( IND_START_DATE ) AS IND_START_DATE,
		MAX( IND_END_DATE ) AS IND_END_DATE 
	FROM
		IND_DIALYSIS_ADD S 
		CROSS JOIN CONVERT_HISTORY  CH
	WHERE
		S.UP_DATE > CH.CONVERT_DATETIME
	GROUP BY
		PATID 
	) R  
UNION ALL
SELECT
	 R.PATID 
FROM
	(
	SELECT
		PATID,
		MIN( IND_START_DATE ) AS IND_START_DATE,
		MAX( IND_END_DATE ) AS IND_END_DATE 
	FROM
		IND_DIALYSIS_COND S 
		CROSS JOIN CONVERT_HISTORY  CH
	WHERE
		S.UP_DATE > CH.CONVERT_DATETIME
	GROUP BY
		PATID 
	) R
UNION ALL
SELECT
	 R.PATID 
FROM
	(
	SELECT
		PATID,
		MIN( IND_START_DATE ) AS IND_START_DATE,
		MAX( IND_END_DATE ) AS IND_END_DATE 
	FROM
		IND_DIALYSIS_EQUIP S 
		CROSS JOIN CONVERT_HISTORY  CH
	WHERE
		S.UP_DATE > CH.CONVERT_DATETIME
	GROUP BY
		PATID 
	) R
UNION ALL
SELECT
	 R.PATID
FROM
	(
	SELECT
		PATID,
		MIN( IND_START_DATE ) AS IND_START_DATE,
		MAX( IND_END_DATE ) AS IND_END_DATE 
	FROM
		IND_DIALYSIS_MEDI S
		CROSS JOIN CONVERT_HISTORY  CH
	WHERE
		S.UP_DATE > CH.CONVERT_DATETIME
	GROUP BY
		PATID 
	) R
UNION ALL
SELECT
	 R.PATID
FROM
	(
	SELECT
		PATID,
		MIN( IND_START_DATE ) AS IND_START_DATE,
		MAX( IND_END_DATE ) AS IND_END_DATE 
	FROM
		IND_DIALYSIS_PLAN S 
		CROSS JOIN CONVERT_HISTORY  CH
	WHERE
		S.UP_DATE > CH.CONVERT_DATETIME
	GROUP BY
		PATID 
	) R
UNION ALL
SELECT
     PATID
FROM
    IND_PRESCRIPTION S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
     PATID
FROM
    PAT_REVISE_OFFWATER S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
UNION ALL
SELECT
     PATID
FROM
    PAT_REVISE_TARE S
	CROSS JOIN CONVERT_HISTORY  CH
WHERE
    S.UP_DATE > CH.CONVERT_DATETIME GROUP BY PATID
	) A
	GROUP BY PATID