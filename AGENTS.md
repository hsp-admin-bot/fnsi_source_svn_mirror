# プロジェクト

このプロジェクト（PJ名：FNSi）は人工透析の電子カルテアプリである。

## クロスリポジトリ

- C:\FNW\fnsi_local
  - 開発用リポジトリでGit管理。ビルド実行ファイルやアプリの実行ログなどが吐き出されている。
    - menu.ps1
      - 中核となるファイル。アプリのビルドや設定変更を一気通貫で行う。
    - \efs
      - アプリのログディレクトリ。local DBで接続設定している時はこちらを参照する。
    - \work
      - local DBとして接続している際の電文ファイルを格納している。
    - \dev_debug\Source
      - `C:\FNW\Source`のミラーフォルダ。開発ファイルのGitプッシュ用。ファイルを格納する時は`C:\FNW\Source`と同一のディレクトリ階層にしている。
- C:\FNW\Source
  - リリース済みソース。SVN管理。ソース解析はこのフォルダ。
    - \ntss-src
      - メインフォルダ
    - \ntss-win
      - 個別機能として独立したツールなど

## 設計思想

連携先のメーカーによって連携する電文が異なるためVueやJAVAなどのソースコードは全て汎用的にコーディングされている。
外部システム連携設定 * 施設設定の組み合わせで処理・出力内容が異なる。
docs\ai配下のmdファイルを参照すること。

## 共通禁止事項

基本的にはコードベースでソース捜査を行うべきだが、やむを得ずファイル検索をターミナルコマンドで実施する際には`.augmentignore`の指定ファイルを除外して検索すること
